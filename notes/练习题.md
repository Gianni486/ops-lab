

---

## 一、两套日志体系（18 分）

**1.（10 分）** CentOS 7 里其实有**两套**日志在跑：`journalctl` 管的那套，和 `/var/log/` 下面那堆文件。用自己的话说：
- `journalctl` 比直接 `cat /var/log/messages` **方便在哪**？（至少写出 **2 条**）
- 既然更方便，**为什么 /var/log 到现在还没被淘汰**？（至少 1 条）

**2.（8 分）判断 + 一句话说明**：`/var/log/journal/` 下面的日志是纯文本，可以直接 `cat` 或者 `grep` 里头的关键字。

---

## 二、查日志，命令要写得出来（每题 8 分，共 32 分）

服务名统一用 `nginx`，写出**完整命令**。

**3.** 只看 nginx 这一个服务的日志。

**4.** 想实时盯着它的新输出（`tail -f` 那种效果）。

**5.** 只看**今天 14:00 到 15:00** 之间、且**级别在 error 及以上**的 nginx 日志。

**6.** 这台机器昨天夜里崩过一次、自动重启了。你想看**它重启之前那一次开机期间**，nginx 到底为什么挂的。

---

## 三、/var/log 里都住了谁（12 分）

**7.** 下面的情况该翻哪个文件？（写出**文件名**，CentOS 7）

| 场景 | 文件 |
|---|---|
| ① 怀疑有人暴力破解 SSH，想看登录失败记录 | `/var/log/______` |
| ② 系统杂事记录，出问题先翻这本（主日志） | `/var/log/______` |
| ③ 硬件、驱动、内核启动信息 | `/var/log/______`（或直接用命令 ______） |
| ④ 定时任务到底有没有按时跑 | `/var/log/______` |

---

## 四、日志轮转 logrotate（38 分）

**8.（12 分）**
- logrotate 自己的配置文件放在**哪两个地方**？
- **谁**每天定时去调用它？（精确到目录里的那个文件）

**9.（14 分）** 下面是一个 nginx 的常见轮转配置，补全并解释：

```conf
/var/log/nginx/*.log {
    daily
    ①                  # 保留几份历史日志（填字段名即可）
    ②                  # 把旧日志压缩，省空间
    missingok
    notifempty
    ③                  # 轮转后创建新文件，权限 0640，属主 nginx
    ④                  # 这一对关键字叫什么？里面为什么通常要写 nginx -s reopen
}
```

**10.（12 分）** 场景：logrotate 配好了，日志**确实每天切出了新文件** `xxx.log.1`，**但服务还在往已经被改名的旧文件里写**，新生成的文件一直是 0 字节。
问：为什么会这样？有哪**两种**解决办法？

---

## 加分题（10 分）

**11.** 接到磁盘告警，你 `du` 一圈发现 `/var/log/journal/` 占了 5 个 G。
问：怎么**安全地**把它清掉（而不是 `rm -rf` 一把梭）？以及怎么设置让它**以后别再无限涨**？（写出命令）

> 提示：这道题跟周六要做的"故障一：磁盘写满"是同一件事，提前想一遍。

---

1./var/log和log下的journal日志本质是一样但是journal是二进制文件单纯用管道指令检索不出什么
2.journal的日志都是基于systemd相关，比如查找自我创建的日志e和应用本身的日志
3.journalctl -u mysql -f
4.journalctl -p warning -b 1
5.journalctl -u sshd --since "20:00" --until "08:00" -p err
6.journalctl -u docker -b -1 -e
7.①cron ②secure ③demerge ④messages ⑤secure ⑥不知道
8./etc/logrotate.conf;/etc/logrotate.d;/etc/daily.d/logrotate
9.create权限写漏了会导致轮转配置文件出错，还有一处不清楚
10.可能是logrotate配置文件出错；可以推翻配置重写，但是时间成本过高，也可以进行逐一排查，这个代价我说不出来
11.不清楚